local ZoneConfig = {
    hubConfig = {
        listenIP = "127.0.0.1",
        listenPort = 50000,
    },
    mysqlConfig = {
        listenIP = "127.0.0.1",
        listenPort = 3306,
    },

    redisConfig = {
    },
}

return ZoneConfig
